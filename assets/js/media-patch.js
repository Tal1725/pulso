import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.115.0/+esm';
const supabase=createClient('https://vqpavcyehgdifbtvzhcn.supabase.co','sb_publishable_915zO84U7fk0ZAjE4vdsFQ_yRWDA6Cm',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const $=s=>document.querySelector(s);let user=null;
function injectCss(){if($('#mediaPatchCss'))return;const s=document.createElement('style');s.id='mediaPatchCss';s.textContent='.media-card{max-width:620px}.camera-preview{width:100%;max-height:58vh;object-fit:cover;border-radius:18px;background:#000}.media-timer{text-align:center;font-weight:700;margin:12px 0}.media-actions{display:flex;gap:10px;justify-content:center;flex-wrap:wrap}.upload-icon{cursor:pointer;user-select:none}.upload-icon input{display:none}';document.head.appendChild(s)}
async function renderMedia(){const{data:p}=await supabase.from('posts').select('id,media_type,media_url,video_url').not('media_type','is',null).limit(100);if(!p?.length)return;const map=new Map(p.map(x=>[x.id,x]));document.querySelectorAll('[data-post]').forEach(card=>{const x=map.get(card.dataset.post);if(!x||!x.media_type||!x.media_url)return;const old=card.querySelector('.video');if(x.media_type==='video'){if(old){old.src=x.media_url;old.setAttribute('playsinline','');}}else{if(old){const el=x.media_type==='image'?document.createElement('img'):document.createElement('audio');el.className='video';el.src=x.media_url;el.controls=x.media_type==='audio';el.playsInline=true;el.style.width='100%';el.style.maxHeight='70vh';el.style.objectFit=x.media_type==='image'?'cover':'initial';old.replaceWith(el)}}})}
async function init(){injectCss();const{data:s}=await supabase.auth.getSession();user=s.session?.user;if(!user)return;const feed=$('#feed');if(feed){new MutationObserver(()=>renderMedia()).observe(feed,{childList:true,subtree:true});setTimeout(renderMedia,400)}}
window.addEventListener('load',init);