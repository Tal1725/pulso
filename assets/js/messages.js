import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.115.0/+esm';
const sb=createClient('https://vqpavcyehgdifbtvzhcn.supabase.co','sb_publishable_915zO84U7fk0ZAjE4vdsFQ_yRWDA6Cm',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const esc=v=>String(v??'').replace(/[&<>\\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\\':'&#92;','"':'&quot;',"'":'&#39;'}[m]));
let me=null,current=null,channel=null,peopleCache=[];
const modal=()=>document.getElementById('messagesModal');
const setMsg=(text,ok=false)=>{const el=document.getElementById('messageStatus');if(el){el.textContent=text;el.className=ok?'message-status ok':'message-status'}};
const isMutual=async id=>{if(!me||!id||id===me)return false;const{data,error}=await sb.from('follows').select('follower_id,following_id').or(`and(follower_id.eq.${me},following_id.eq.${id}),and(follower_id.eq.${id},following_id.eq.${me})`);if(error){console.error('mutual check',error);return false}return(data||[]).length===2};
async function init(){const{data,error}=await sb.auth.getSession();if(error){console.error('messages session',error);return}me=data.session?.user?.id;if(!me)return;bind();await loadPeople();startRealtime()}
function bind(){document.getElementById('messagesBtn')?.addEventListener('click',open);document.getElementById('messagesClose')?.addEventListener('click',close);document.getElementById('messageSend')?.addEventListener('click',send);document.getElementById('messageInput')?.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}})}
async function loadPeople(){const box=document.getElementById('messagePeople');if(!box||!me)return;const{data:following,error:fErr}=await sb.from('follows').select('following_id').eq('follower_id',me);if(fErr){console.error('following list',fErr);box.innerHTML='<div class="message-empty">Não foi possível carregar seus contatos.</div>';return}const ids=(following||[]).map(x=>x.following_id);if(!ids.length){peopleCache=[];box.innerHTML='<div class="message-empty">💬 Para trocar mensagens, vocês precisam seguir um ao outro.</div>';return}const{data:back,error:bErr}=await sb.from('follows').select('follower_id').eq('following_id',me).in('follower_id',ids);if(bErr){console.error('mutual list',bErr);box.innerHTML='<div class="message-empty">Não foi possível verificar seus contatos.</div>';return}const mutualIds=new Set((back||[]).map(x=>x.follower_id));const allowed=ids.filter(id=>mutualIds.has(id));if(!allowed.length){peopleCache=[];box.innerHTML='<div class="message-empty">💬 Nenhum contato conectado ainda.<br><small>Quando vocês se seguirem, a conversa aparecerá aqui.</small></div>';return}const{data,error}=await sb.from('profiles').select('id,display_name,username,avatar_url,member_number').in('id',allowed).order('display_name');if(error){console.error('messages people',error);box.innerHTML='<div class="message-empty">Não foi possível carregar os contatos.</div>';return}peopleCache=data||[];box.innerHTML=peopleCache.map(p=>`<button class="message-person" data-person="${p.id}" type="button"><span class="message-avatar">${p.avatar_url?`<img src="${esc(p.avatar_url)}" alt="">`:esc((p.display_name||'?')[0].toUpperCase())}</span><span><b>${esc(p.display_name||'Usuário')}</b><small>@${esc(p.username||'membro')} · #${String(p.member_number||0).padStart(6,'0')} · 🟢 Conectado</small></span></button>`).join('')}
async function selectPerson(p){if(!p||!(await isMutual(p.id))){setMsg('Vocês precisam seguir um ao outro para trocar mensagens.');return false}current=p;document.getElementById('messageTitle').textContent=`💬 ${p.display_name||'Usuário'}`;document.getElementById('messagePeople').hidden=true;document.getElementById('messageThread').hidden=false;setMsg('');await refresh();document.getElementById('messageInput')?.focus();return true}
async function openConversation(id){if(!me)return false;const ok=await isMutual(id);if(!ok){alert('Para enviar mensagem, vocês precisam seguir um ao outro no PULSO.');return false}if(!peopleCache.length)await loadPeople();const p=peopleCache.find(x=>x.id===id);if(!p)return false;modal().hidden=false;return await selectPerson(p)}
async function refresh(){if(!me||!current)return;const{data,error}=await sb.from('direct_messages').select('id,sender_id,receiver_id,body,created_at,read_at').or(`and(sender_id.eq.${me},receiver_id.eq.${current.id}),and(sender_id.eq.${current.id},receiver_id.eq.${me})`).order('created_at',{ascending:true});if(error){console.error('messages refresh',error);setMsg('Erro ao carregar a conversa: '+(error.message||'tente novamente'));return}const thread=document.getElementById('messageThread');if(!thread)return;thread.innerHTML=(data||[]).map(x=>`<div class="message-bubble ${x.sender_id===me?'mine':'theirs'}">${esc(x.body)}<small>${new Date(x.created_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}${x.sender_id===me?(x.read_at?' · ✓✓':' · ✓'):''}</small></div>`).join('')||'<div class="message-empty">Ainda não há mensagens. Comece a conversa. 👋</div>';thread.scrollTo({top:thread.scrollHeight,behavior:'smooth'});const unread=(data||[]).filter(x=>x.receiver_id===me&&!x.read_at).map(x=>x.id);if(unread.length){const r=await sb.from('direct_messages').update({read_at:new Date().toISOString()}).in('id',unread);if(r.error)console.error('messages read',r.error)}}
async function send(){const input=document.getElementById('messageInput'),body=input?.value.trim();if(!body||!current)return;if(!(await isMutual(current.id))){setMsg('A conversa foi bloqueada porque vocês não se seguem mutuamente.');return}const receiver=current.id;input.value='';setMsg('Enviando...');const{error}=await sb.from('direct_messages').insert({sender_id:me,receiver_id:receiver,body});if(error){console.error('messages send',error);input.value=body;setMsg(error.code==='42501'?'Vocês precisam seguir um ao outro para conversar.':'Não foi possível enviar: '+(error.message||'verifique sua conexão'));return}setMsg('Enviada ✓',true);await refresh()}
function startRealtime(){if(!me)return;channel=sb.channel(`pulso-messages:${me}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_messages',filter:`receiver_id=eq.${me}`},payload=>{if(current&&payload.new.sender_id===current.id)refresh();else loadPeople()}).subscribe(status=>{if(status==='CHANNEL_ERROR')console.warn('messages realtime channel error')})}
async function open(){modal().hidden=false;document.getElementById('messagePeople').hidden=false;document.getElementById('messageThread').hidden=true;document.getElementById('messageTitle').textContent='💬 Mensagens';document.getElementById('messageInput').value='';setMsg('');current=null;await loadPeople()}
function close(){modal().hidden=true;current=null}
window.pulsoOpenMessage=openConversation;
window.addEventListener('load',init);