import {createClient} from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.115.0/+esm';
const supabase=createClient('https://vqpavcyehgdifbtvzhcn.supabase.co','sb_publishable_915zO84U7fk0ZAjE4vdsFQ_yRWDA6Cm',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function icon(type){return type==='like'?'❤️':type==='follow'?'➕':'💬'}
function text(type,name){return type==='like'?`${name} curtiu seu vídeo`:type==='follow'?`${name} começou a seguir você`:`${name} comentou no seu vídeo`}
async function loadNotifications(){const {data:session}=await supabase.auth.getSession();const uid=session.session?.user?.id;if(!uid)return;const {data,error}=await supabase.from('notifications').select('id,actor_id,type,post_id,created_at,read_at').eq('recipient_id',uid).order('created_at',{ascending:false}).limit(40);if(error)return;const ids=[...new Set((data||[]).map(n=>n.actor_id))];let map=new Map();if(ids.length){const {data:ps}=await supabase.from('profiles').select('id,display_name,username').in('id',ids);map=new Map((ps||[]).map(p=>[p.id,p]))}const unread=(data||[]).filter(n=>!n.read_at).length;const badge=$('#notificationBadge');if(badge){badge.textContent=unread>99?'99+':unread;badge.hidden=unread===0}const body=$('#notificationBody');if(!body)return;if(!data?.length){body.innerHTML='<div class="empty">Você ainda não tem notificações.</div>';return}body.innerHTML=data.map(n=>{const p=map.get(n.actor_id)||{};return `<div class="notification-row ${n.read_at?'':'unread'}"><span class="notification-icon">${icon(n.type)}</span><div><strong>${esc(p.display_name||'Usuário')}</strong><span>${esc(text(n.type,p.display_name||'Alguém'))}</span><small>${new Date(n.created_at).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'})}</small></div></div>`}).join('')}
async function markRead(){const {data:session}=await supabase.auth.getSession();const uid=session.session?.user?.id;if(!uid)return;await supabase.from('notifications').update({read_at:new Date().toISOString()}).eq('recipient_id',uid).is('read_at',null);loadNotifications()}
function open(){const m=$('#notificationModal');if(m){m.hidden=false;markRead()}}function close(){const m=$('#notificationModal');if(m)m.hidden=true}
window.addEventListener('load',()=>{const b=$('#notificationBtn');if(b)b.onclick=open;const c=$('#notificationClose');if(c)c.onclick=close;const m=$('#notificationModal');if(m)m.addEventListener('click',e=>{if(e.target===m)close()});loadNotifications();setInterval(loadNotifications,20000)});